[gcode_macro NG_Home]
gcode:
    G28 Z                      ; do the coarse home
    G90                        ; set absolute positioning
    G1 Z2 F600                ; to 2mm -- home height (or whatever you'd like), for maximum sensor accuracy
    G4 S1                      ; chill for a sec
    M400                       ; wait for move to finish
    PROBE_EDDY_NG_PROBE_STATIC HOME_Z=1 ; read the current exact height from sensor and home Z to it
    G0 Z4 F600 ; to 5mm as home
    M400
    PROBE_EDDY_NG_TAP